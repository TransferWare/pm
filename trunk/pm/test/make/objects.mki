$(PROJECTNAME)_VERSION = $(call file_contents,$(WKTOP)/../VERSION)

SCHEMA := pm_owner

MANIFEST_OBJECTS :=

# All ut_<packages>
UT_TARGETS := 

OBJECTS_SCRIPTS := \
	$(addprefix ut_,$(addsuffix .pks,$(UT_TARGETS))) \
	$(addprefix ut_,$(addsuffix .pkb,$(UT_TARGETS))) \
	$(ENDOFLIST)

include $(CFGCOMMONDIR)/test_defs.mki

include $(CFGCOMMONDIR)/objects.mki
